"Tilt core utility functions."

def watch_files(file_paths):
    """Watches a list of files.

    If a file is changed a re-exectution of the Tiltfile is triggered.
    If a path is a directory, its contents will be recursively watched.
    """
    for f in file_paths:
        watch_file(f)
